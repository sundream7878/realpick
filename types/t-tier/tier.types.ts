// Tier system type definitions for RealPick
export interface TTierInfo {
  name: string
  minPoints: number
  characterImage: string
}

// Legacy Type Export (하위 호환성)
export type TierInfo = TTierInfo

