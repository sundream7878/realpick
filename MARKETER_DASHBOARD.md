# 리얼픽 마케팅 봇 대시보드 설정 가이드

**작성일**: 2026-02-06

## 📌 개요

- 관리자 페이지(`/admin`)의 "리얼픽 마케터" 탭과 동일한 디자인/기능의 독립 대시보드 구축
- 위치: `realpick-marketing-bot/dashboard/`
- 목적: 마케팅 자동화 작업을 별도 대시보드에서 관리

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│  리얼픽 마케팅 봇 대시보드 (localhost:5173)               │
│  - Vite + React                                          │
│  - 관리자 페이지와 동일한 UI/UX                           │
└───────────────────┬─────────────────────────────────────┘
                    │ API 호출 (/api/...)
                    ↓ Vite Proxy
┌─────────────────────────────────────────────────────────┐
│  Next.js 앱 (localhost:3000)                            │
│  - API Routes (/api/admin/marketer/...)                 │
│  - Firebase Admin SDK                                    │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ↓
┌─────────────────────────────────────────────────────────┐
│  Firebase (Firestore, Storage, Auth)                    │
└─────────────────────────────────────────────────────────┘
```

## 📂 디렉토리 구조

```
realpick-marketing-bot/
├── dashboard/              # 독립 대시보드
│   ├── src/
│   │   ├── components/    # UI 컴포넌트
│   │   │   ├── AutoMissionGenerate.tsx
│   │   │   ├── YoutubeDealerRecruit.tsx
│   │   │   ├── InstagramViralManage.tsx
│   │   │   ├── FakeUserBotManage.tsx
│   │   │   ├── CommunityViralManage.tsx
│   │   │   └── NaverCafeCrawl.tsx
│   │   ├── lib/
│   │   │   └── shows.ts   # 프로그램 상수
│   │   ├── hooks/
│   │   │   └── useToast.ts
│   │   └── App.tsx
│   ├── vite.config.ts     # Vite 설정 (Proxy 포함)
│   └── package.json
│
├── backend/               # 백엔드 서버 (선택적)
│   ├── src/
│   │   ├── server.ts
│   │   └── routes/
│   └── package.json
│
└── .env.local            # 환경 변수
```

## 🔧 설정 과정

### 1. 컴포넌트 복사 및 Import 경로 수정

관리자 페이지의 모든 컴포넌트를 대시보드로 복사하고 import 경로 수정:

```typescript
// 수정 전 (Next.js)
import { Card } from "@/components/c-ui/card"
import { useToast } from "@/hooks/h-toast/useToast.hook"
import { SHOWS } from "@/lib/constants/shows"

// 수정 후 (Vite)
import { Card } from "./ui/card"
import { useToast } from "../hooks/useToast"
import { SHOWS } from "../lib/shows"
```

### 2. Firebase 직접 호출 제거

대시보드는 Firebase에 직접 접근하지 않고 Next.js API를 통해서만 통신:

```typescript
// 제거: Firebase 직접 호출
const { auth } = await import("@/lib/firebase/config")
const { doc, updateDoc } = await import("firebase/firestore")

// 변경: API 호출
const response = await fetch("/api/admin/marketer/...")
```

### 3. Vite Proxy 설정

`vite.config.ts`에 프록시 추가하여 CORS 문제 해결:

```typescript
export default defineConfig({
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        secure: false,
      }
    }
  }
})
```

### 4. API 경로 통일

모든 API 호출을 상대 경로로 변경:

```typescript
// 절대 경로 제거
fetch("http://localhost:3000/api/...")

// 상대 경로로 변경 (Vite Proxy가 자동으로 localhost:3000으로 전달)
fetch("/api/admin/marketer/...")
```

## 🚀 실행 방법

3개의 서버를 순서대로 실행:

```powershell
# 1. Next.js 메인 앱 (포트 3000)
cd f:\realpick
npm run dev

# 2. 백엔드 서버 (포트 3001) - 선택적
cd f:\realpick\realpick-marketing-bot\backend
npm run dev

# 3. 대시보드 (포트 5173)
cd f:\realpick\realpick-marketing-bot\dashboard
npm run dev
```

접속:
- **대시보드**: http://localhost:5173
- **관리자 페이지**: http://localhost:3000/admin (리얼픽 마케터 탭)

## 🔑 환경 변수

`.env.local` (대시보드용):
```env
VITE_ADMIN_PASSWORD=realpick-admin-2024
```

## ⚠️ 주의사항

### 1. 한글 인코딩 문제
- PowerShell에서 파일 수정 시 UTF-8 인코딩 필수
- 대량 치환 시: `Set-Content -Encoding UTF8`

### 2. 세미콜론 필수
- Babel 파서가 엄격하므로 모든 문장 끝에 세미콜론 추가
- `} else throw` → `} else { throw ... }`

### 3. API 경로 일관성
- 대시보드: `/api/...` (상대 경로)
- Vite Proxy → Next.js: `localhost:3000/api/...`

### 4. 포트 충돌 해결
```powershell
# 포트 사용 중인 프로세스 종료
Get-NetTCPConnection -LocalPort 3001 | 
  Select-Object -ExpandProperty OwningProcess | 
  ForEach-Object { Stop-Process -Id $_ -Force }
```

## 📝 완료된 작업

- ✅ 모든 컴포넌트 복사 및 import 경로 수정
- ✅ Firebase 직접 호출 → API 호출로 변경
- ✅ Vite Proxy 설정으로 CORS 해결
- ✅ 한글 인코딩 및 구문 에러 수정
- ✅ 헤더 디자인 통일 ("리얼픽 마케터" + "Marketing Automation System")

## 🚧 남은 작업

- [ ] 대시보드 실행 테스트
- [ ] 모든 기능 동작 확인 (미션 생성, 크롤링, 봇 관리 등)
- [ ] 에러 핸들링 강화
- [ ] 백엔드 서버 라우트 추가 (필요시)

## 🐛 문제 해결

### "Failed to fetch" 에러
- 원인: CORS 또는 서버 미실행
- 해결: Vite Proxy 설정 + Next.js 앱 실행 확인

### "Unexpected token" JSON 에러
- 원인: API가 HTML 반환 (서버 미실행)
- 해결: localhost:3000 서버 실행 확인

### 한글 깨짐
- 원인: PowerShell 인코딩 문제
- 해결: UTF-8 인코딩으로 파일 재저장
